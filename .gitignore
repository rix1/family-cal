_chat.txt
*.csv
!seed/*.csv
# Real family PII — keep the people file local only (groups/viewers seeds stay tracked).
seed/people.csv

# Local secrets / config — the template stays tracked.
.env
!.env.template

# Generated / dependency output
_fresh/
_prod/
node_modules/
.deploy-commit

# Local Deno KV databases
*.db
*.db-shm
*.db-wal
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
.data/
